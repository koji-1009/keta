name: keta_shelf
description: >-
  Bidirectional bridge between keta and package:shelf — mount a keta App as a
  shelf handler, or run a shelf handler inside keta.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  keta: ^0.1.0
  shelf: ^1.4.0

dev_dependencies:
  test: ^1.25.0
