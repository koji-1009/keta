name: keta_openapi
description: >-
  Ring 1 for keta: the route-table walk that emits an OpenAPI 3.1 document from
  the RouteDoc/Schema declarations owned by keta core. Pure derivation.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  keta: ^0.1.0

dev_dependencies:
  test: ^1.25.0
  yaml: ^3.1.0
