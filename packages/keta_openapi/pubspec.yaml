name: keta_openapi
description: >-
  Ring 2 for keta: the Schema constant type, RouteDoc, and the route-table walk
  that emits OpenAPI 3.1 from runtime values.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  keta: ^0.1.0

dev_dependencies:
  test: ^1.25.0
  yaml: ^3.1.0
