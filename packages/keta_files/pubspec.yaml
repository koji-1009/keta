name: keta_files
description: >-
  File-convention entry for keta: discovers route files under a directory and
  materializes their registrations into a manifest's marked regions.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  keta: ^0.1.0
  analyzer: ^14.0.0
  dart_style: ^3.1.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
